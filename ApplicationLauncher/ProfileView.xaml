<UserControl x:Class="ApplicationLauncher.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApplicationLauncher"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FFC9DAFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <!-- Row for the header -->

            <RowDefinition Height="*"/>
            <!-- Row for the content below -->
        </Grid.RowDefinitions>

        <!-- Nested Grid for the Header in the First Row -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Label centered in the second column of the nested grid -->
            <Label x:Name="ProfileLabel" Content="Profiles" FontSize="20" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <!-- New Profile button in the third column of the nested grid -->
            <Button x:Name="NewProfileButton" Content="New Profile +" Grid.Column="2" HorizontalAlignment="Right" Margin="0,0,10,0" VerticalAlignment="Center" Height="34" Width="90" Click="NewProfileButton_Click"/>
        </Grid>

        <Grid Grid.Row="1" Background="#FFB4B4B4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="10"/>
            </Grid.RowDefinitions>

            <!-- ScrollViewer for dynamic profile content -->
            <ScrollViewer Grid.Row="1" Grid.Column="1">
                <ItemsControl x:Name="profilesPanel" HorizontalContentAlignment="Stretch">
                    <!-- Each row will be added via code when reading in the csv file -->
                </ItemsControl>
            </ScrollViewer>
        </Grid>


    </Grid>
</UserControl>
